# ── CNN Training Dependencies ──────────────────────────────────────────────
# Install on your PC (not on the Pi):
#   pip install -r models/cnn/requirements.txt

tensorflow>=2.13
tensorboard          # required on Windows — TF doesn't auto-pull it
numpy
pillow
matplotlib
scikit-learn
